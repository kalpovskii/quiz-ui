import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Extend here (e.g. transpilePackages, images.remotePatterns for
  // QuizImageChoice's <img> sources) as the docs site grows.
};

export default nextConfig;

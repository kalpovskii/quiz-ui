# server/

Mirrors 8bitcn-ui's top-level `server/` — server actions / server-only
logic for the site (not the registry). Empty for the same reason as
`db/`: nothing in quiz-ui's distribution mechanism needs it yet. Add
server actions here as the docs site grows (e.g. a contact form, search
indexing for docs, or fetching the star count for the homepage).
